version: "3.8"
services:
  api:
    build: .
    ports:
      - "11001:11001"
    environment:
      - NODE_ENV=production
      - PORT=11001
      - MONGODB_URI=${MONGODB_URI}
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - RATE_LIMIT_MAX=${RATE_LIMIT_MAX}
